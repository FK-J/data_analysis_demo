# 可选：如果不想把密码直接写入 configs/database.yaml，可以使用环境变量。
# 将本文件复制为 .env 后填写真实值；.env 禁止提交。

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=your_database
